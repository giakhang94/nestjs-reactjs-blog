version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: nestjs-blog-mysql
    volumes:
      - nestjs-blog-mysql:/var/lib/
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}

volumes:
  nestjs-blog-mysql:
